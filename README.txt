To run this app you need:

- Ruby with DevKit
  Install Bundler & SASS using "gem   
  install <name>" command in Command   Prompt with Ruby.exe (Windows)
- Bootstrap 4, JQuery, PopperJS
- VSCode + Live Server Extension to run the project.

* This project is using Git